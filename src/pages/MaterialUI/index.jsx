import { Button } from "@mui/material"

const MaterialUI = ({ }) => {
    return (
        <>
            <Button style={{ backgroundColor: 'black' }} variant="text">click me</Button>
        
        </>
    )
}
export default MaterialUI
